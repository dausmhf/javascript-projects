// Define a variable to hold the name
let userName = "alice";
// Greeting Message for define variable
console.log("Hello " + userName)

// Reassign the variable to a new name
userName = "bob";
// Greeting Message for reassign variable
console.log("Hello " + userName)